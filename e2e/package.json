{
  "name": "icsop-e2e",
  "private": true,
  "description": "Prototype-fidelity acceptance ring (Playwright). Uncle-Bob constraint #1: acceptance tests derived from prototypes, blind to implementation, run against the live stack.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:list": "playwright test --list",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
