/**
 * 檢視器／預覽頁之安全說明文案（2026-09-23 全站文案稽核）。
 *
 * 🔴 使用者裁決：**實作邏輯與技術細節不得常駐於 UI；如需保留一律以 hover（ⓘ）承載。**
 * 🔒 分流判準＝**這句話在回答使用者的哪個問題**（非「技術性與否」）：
 *    「這份文件上為什麼有我的名字／我下載後會怎樣」⇒ 可見或 ⓘ；
 *    「浮水印是誰產生的、用什麼方式畫出來的」⇒ **刪除**（那是實作）。
 *
 * 📝 已作廢之原文案逐字保留於各呼叫端之註解中（⚠ 不得復原）。
 */

/** 檢視器常駐可見句——只說事實，不說機制。 */
export const VIEWER_SECURITY_TEXT = '本文件已標記您的身分浮水印。';

/**
 * 檢視器 ⓘ 內容。
 * ⚠ 第二段是**對使用者的承諾**（下載所得與畫面一致），不是實作細節，故保留。
 */
export const VIEWER_SECURITY_NOTE = [
  '浮水印包含您的身分與開啟時間，於您每次開啟時產生。',
  '下載或列印所得的檔案與此畫面完全一致，浮水印在離開本系統後仍然存在。',
] as const;

/** 樹狀圖預覽（後台循環／業務功能類別）之常駐可見句。 */
export const TREE_PREVIEW_SECURITY_TEXT = '本預覽已標記您的身分浮水印，並記錄本次調閱。';

/** 樹狀圖預覽之 ⓘ 內容。 */
export const TREE_PREVIEW_SECURITY_NOTE = [
  '浮水印包含您的身分與開啟時間，於您每次開啟時產生。',
  '下載或列印時會另外各記錄一次，所得檔案與此畫面所見一致。',
] as const;

/** 前台業務/功能類別樹狀圖之常駐可見句。 */
export const PUBLIC_TREE_SECURITY_TEXT = '本頁已標記您的身分浮水印。';

/**
 * 前台業務/功能類別樹狀圖之 ⓘ 內容。
 * 🔒 「何時才會留下調閱紀錄」對使用者是有意義的資訊（他要知道自己的行為何時被記錄），
 *    故保留；但不再以「不記錄調閱稽核」這種系統事件語言陳述。
 */
export const PUBLIC_TREE_SECURITY_NOTE = [
  '浮水印包含您的身分與開啟時間，於您每次開啟時產生。',
  '瀏覽樹狀圖、切換類別與開啟節點清單不會留下調閱紀錄；點入程序書開啟檢視器時才會記錄一次。',
] as const;

/** 浮水印字串對照之標籤（原為「浮水印格式（與稽核快照一致）：」）。 */
export const WATERMARK_SAMPLE_LABEL = '您的浮水印：';
